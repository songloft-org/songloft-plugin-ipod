import { ASSETS } from "@/assets";

/** Backward-compatible app URL for paths handled by the Songloft WebView. */
export const APP_URL = ".";
export const DEFAULT_ARTWORK_URL = ASSETS.defaultAlbumArtwork;
